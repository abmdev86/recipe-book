import { Inter, Lusitana, Young_Serif, Outfit } from "next/font/google";

export const young_serif = Young_Serif({
  subsets:['latin'], weight:['400']
});

export const outfit = Outfit({
    weight: ['400', '700'],
    subsets: ['latin'],
  });

  // export const young_serif = Young_Serif({
  //   subsets:['latin'], weight:['400']
  // });